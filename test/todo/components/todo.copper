
<!-- <cu-todo>
	<h2>Todo ({{ todo.length }})</h2>
	<!--
	<cu if="todo.length > 0">
		Todo list here...
		<!-- <todo-list
			:todo="todo"
			@remove="todo.splice($event, 1)"
			>
		</todo-list> -/->
	</cu>
	<cu else>
		<i>No tasks to do.</i>
	</cu>
</cu-todo> -->

<cu-todo>
	<todo-title :count="todo.length"></todo-title>
	<div>
		counter: {{ counter }}
		<input type="button" value="Increment" @click="counter++">
	</div>
	<div>
		<input
			type="text"
			:value.sync="new_todo_title"
			>
		<input
			type="button"
			value="Add"
			@click="addTodo()"
			>
	</div>
	<cu if="todo.length > 0">
		<todo-list
			:todo="todo"
			@remove="todo.splice($event, 1)"
			>
		</todo-list>
	</cu>
	<cu else>
		<i>No tasks to do.</i>
	</cu>
</cu-todo>

<script type="module">
	import TodoList from '/test/todo/dist/components/todo-list.js';
	import TodoTitle from '/test/todo/dist/components/todo-title.js';

	let counter = 0;
	let new_todo_title = '';
	let todo = [];

	function addTodo(arg) {
		todo.push({
			id: Date.now(),
			title: new_todo_title,
		});

		new_todo_title = '';
	}
</script>

<!--
<script type="module">
	import TodoTitle from '/test/todo/dist/components/todo-title.js';
	import TodoList from '/test/todo/dist/components/todo-list.js';

	// non-reactive data
	const title = 'Todo';

	// reactive data (refs)
	let new_todo_title = '';
	let todo = [ 1,2,3 ];
	let [ todo0 ] = todo;

	// props
	// validating using valibot, for example
	const { user_id, email } = defineProps({
		user_id: number([ minValue(1) ]),
		email: string([ email() ]),
	});

	// computed
	// no function, just mention the refs in the expression
	const todo_count = todo.length;

	// methods
	function addTodo() {
		todo.push({
			id: Date.now(),
			title: new_todo_title,
		});
		new_todo_title = '';
	}
</script>
-->

<style>
	cu-todo {
		display: block;
		padding: 10px;
		border: 1px solid #ccc;
		border-radius: 5px;
	}
</style>
